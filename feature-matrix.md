# Mutineer BBS Feature Matrix

Review date: 2026-07-13

Scope: end-to-end repository review across documentation, CMake build wiring, core BBS runtime, doors/protocols, Buccaneer interpreted language/toolchain, PLANK, plugins, tests, and deployment assets. This matrix intentionally lists incomplete, simplified, missing, TODO, or unstable items only.

Second-pass focus: DB/schema initialization, deployment command drift, plugin configuration/security, COVE/PLANK runtime claims, editor/file workflows, and user-visible runtime defects missed in the first pass.

Third-pass focus: scheduler/event lifecycle, login/session concurrency, QWK/attachment transfer edge cases, maintenance shell fallbacks, unbound SQL cleanup calls, and shutdown races missed in the earlier reviews.

Fourth/final-pass focus: correct Buccaneer product framing, auth recovery quality, ACS enforcement at object access time, menu flag semantics, file validation/download policy, and remaining config/runtime drift.

Validation snapshot:

- First-five P0 revalidation used a fresh out-of-tree build directory, `build-p0-first5-review/`.
- `cmake -S . -B build-p0-first5-review` passed.
- `cmake --build build-p0-first5-review` passed.
- `ctest --test-dir build-p0-first5-review --output-on-failure` passed 21/21 test targets.
- Targeted checks passed for daemon `--help`, `--bogus`, `-c`, `--config`, `mutineer-initbbs -c ... -y` core plus PLANK table creation, `tests/test_tools_cli.sh build-p0-first5-review`, `test_plugin build-p0-first5-review/plugins/hello.so`, and `tests/run_expect_tests.sh build-p0-first5-review`.

| Priority level | Percentage complete | State | Description | Dependancy |
|---|---:|---|---|---|
| P0 | 100% | Complete | Daemon config CLI now accepts `--config <path>` and `-c <path>`, keeps `conf/mutineer.conf` as the default, and rejects unknown arguments with usage and nonzero exit. | `src/main.c`, `tests/run_expect_tests.sh`, `tests/test_tools_cli.sh` |
| P0 | 100% | Complete | Normal database initialization now applies both `sql/schema.sql` and `sql/plank_schema.sql`; startup and `mutineer-initbbs` validate/report both core and PLANK schemas. | `src/startup.c`, `src/db.c`, `src/tools/mutineer-initbbs.c`, `sql/plank_schema.sql` |
| P0 | 100% | Complete | Expect tests are hermetic: CTest passes the active build dir, the runner creates a private runtime root, initializes a private DB, starts/stops its own daemon on a private port, refuses preexisting daemons, and uses seeded sysop credentials. | `CMakeLists.txt`, `tests/run_expect_tests.sh`, `tests/expect_helpers.exp`, expect tests |
| P0 | 100% | Complete | CLI tool tests now run against the active CMake binary directory instead of a hardcoded `build/` tree. | `CMakeLists.txt`, `tests/test_tools_cli.sh` |
| P0 | 100% | Complete | Plugin unit tests now load the current build's plugin artifact through a CTest-provided path and no longer depend on hardcoded `build/plugins/hello.so`. | `tests/test_plugin.c`, `CMakeLists.txt`, plugin output path |
| P0 | 100% | Complete | Buccaneer is built by top-level CMake as an internal library/tool target, linked into `mutineer`, and exposed through `runner=bucc` door dispatch using the door record's manifest path. | `CMakeLists.txt`, `src/doors.c`, `src/buccaneer/bbs.c`, Buccaneer docs |
| P0 | 100% | Complete | Buccaneer host dispatch now enforces an allowed-capability mask before handler execution while preserving handler lookup, argument validation, and throttling behavior; tests cover denied write and allowed safe calls. | `src/buccaneer/host.c`, `src/buccaneer/include/bucc_host.h`, `tests/test_buccaneer_host.c` |
| P0 | 100% | Complete | Native doors and transfer protocols now launch through argv parsing plus `fork`/`execvp`, reject shell metacharacters, supervise child exit status, restore node activity, and remove DOSBox cleanup shelling. | `src/doors.c`, `src/file_cmds.c`, `tests/test_doors.c`, protocol/door docs |
| P1 | 35% | Incomplete | Scheduled and logon events execute arbitrary command strings through `system()`, while the Event Editor also accepts a `permission` event type that no scheduler/runtime path consumes. Event warnings can also repeat every scheduler tick during the warning window. This makes the event subsystem both over-permissive and semantically incomplete. | `src/scheduler.c`, `src/session.c`, event editor, event runner policy |
| P1 | 55% | Incomplete | Buccaneer `DOOR.CHAIN` sets the runtime status to `VM_HALT`, not `VM_CHAIN`, so host-call chaining is silently treated as a normal exit. `DOOR.EXIT` reads but discards the provided exit code. | `src/buccaneer/host.c`, `src/buccaneer/vm.c`, `src/buccaneer/bbs.c` |
| P1 | 45% | Incomplete | Buccaneer host API surface is inconsistent with the specs and simulator. `TERM.GOTOXY` is registered but not dispatched; `TERM.PAUSE`, `TERM.SUPPORTS_ANSI`, `TERM.INPUT_PASSWORD`, `SESSION.NODE`, `SESSION.ELAPSED_MS`, and spec spelling `USER.TIME_LEFT` are missing or mismatched. | `src/buccaneer/host.c`, `src/buccaneer/include/bucc_host.h`, `SPECS/BUCCANEER/Buccaneer_SPEC.md` |
| P1 | 50% | Incomplete | Buccaneer runtime has active interpreter defects: host calls silently truncate arguments beyond 16, `OP_ARRAY_NEW` uses `malloc(0)` for empty arrays, `OP_DISPATCH_CALL` casts away `const`, and invalid dispatch selectors leave no result for callers expecting one. | `src/buccaneer/vm.c`, Buccaneer tests, bytecode validation |
| P1 | 45% | Incomplete | Buccaneer `ON CALL` emission always writes `base_proc_id=0`, so dispatch can jump to the wrong procedure set. `OP_RANGE_TEST` is implemented but never emitted by parser/emitter paths. | `src/buccaneer/emit.c`, `src/buccaneer/parser.c`, `src/buccaneer/ast.c`, Buccaneer tests |
| P1 | 35% | Incomplete | Buccaneer embedding setters drop all API context pointers by passing `NULL`; callers cannot safely bind session/user/data/text/BBS APIs through the public runner setters. Convenience runner also grants `BUCC_CAP_ALL` by default. | `src/buccaneer/bbs.c`, `src/buccaneer/include/bucc_bbs.h`, host API implementations |
| P1 | 25% | Incomplete | Plugin configuration keys are documented and present in `conf/mutineer.conf`, but ignored by code. `cfg_load()` has no `plugins_*` fields, and `plugin_loader_init()` ignores `cfg`, always using `plugins/`, enabled when that directory exists, with empty allow/deny lists. Production allowlists, denylists, custom plugin directories, and `plugins_enabled=false` do not work. | `src/config.c`, `include/bbs_config.h`, `src/plugin_loader.c`, `conf/mutineer.conf`, `docs/configuration.md`, `docs/plugins.md` |
| P1 | 30% | Incomplete | Plugin host KV/data directory APIs do not sanitize namespaces, keys, or plugin IDs before joining them into filesystem paths. A plugin can use `../` or absolute-looking path components to read, write, delete, or create files outside the intended `data/plugin_kv` and `data/plugins` roots. | `src/plugin_host_api.c`, plugin ABI docs, filesystem sandbox policy |
| P1 | 45% | Incomplete | Full-screen editor input handling has correctness and safety edge cases. It uses fixed 50x79 buffers with `strcpy`/`strcat`, does not clamp `cur_col` after up/down movement to the new line length, saves with `text_cap - strlen(...)` arithmetic that can underflow when the output buffer fills, and only redraws the first 20 of 50 lines with no scrolling model. | `src/session.c`, message composition paths, full-screen editor tests |
| P1 | 45% | Incomplete | Interactive file upload asks the caller for a server-local source path and copies it into the file area. That is not a real telnet upload path and gives any user who reaches the file area a way to attempt importing arbitrary readable server files by path. The later `cmd_file_upload()` protocol path is better, but this legacy flow remains in `handle_action("files")`. | `src/session.c`, `src/file_cmds.c`, file area ACS/security policy |
| P1 | 40% | Incomplete | Message object access is not consistently re-authorized. Area lists filter by area ACS, but read/reply/edit/thread/attachment flows fetch arbitrary message IDs via `db_message_get()` and then operate on the message's actual `area_id` without checking that area's read/post ACS. Users can potentially read, reply to, edit, or download attachments from messages outside their selected/authorized area if they know an ID. | `src/msg_cmds.c`, `src/db.c`, message area ACS tests |
| P1 | 50% | Incomplete | Message area `acs_read`, `acs_post`, and password fields exist in schema/types but most runtime paths use only the generic `areas[i].acs` list/access value. Posting via `cmd_msg_post()`, `cmd_msg_reply()`, and standalone `fsedit` posting does not enforce `acs_post`; reading does not enforce `acs_read`; area passwords are not prompted. | `include/bbs_db.h`, `src/db.c`, `src/msg_cmds.c`, `src/session.c`, message area editor |
| P1 | 55% | Incomplete | FTN/Fido support is only partially integrated. Netmail and echomail packet file export/import helpers exist, but docs still call full tosser/routing integration pending. There is no complete end-to-end FTN tosser handoff workflow validated by tests. | `src/fido_netmail.c`, `src/session.c`, `docs/PARITY.md`, external tosser tooling |
| P1 | 65% | Incomplete | File transfer support depends on external protocol commands and blocking process waits. Batch and QWK paths launch configured tools, but there is no timeout, cancellation, child cleanup on disconnect, or protocol availability validation comparable to the DOSBox runner. | `src/doors.c`, `src/file_cmds.c`, `src/qwk.c`, protocol config |
| P1 | 50% | Incomplete | File download policy is under-enforced. File listings filter only `acs_list`; `cmd_file_download()`, batch download, and `batchrun` do not enforce the file area's `acs_download`, password, validation flag, free-file flag, or daily download/KB limits from the security level. Users who can list or queue a file can often attempt transfer regardless of download-specific policy. | `src/file_cmds.c`, `src/session.c`, `src/db.c`, file area/security-level policy tests |
| P1 | 35% | Incomplete | QWK download/import paths use predictable `/tmp` directories and shell archive commands. Packet output is `/tmp/<handle>.QWK`, REP processing shells out to `unzip` and `rm`, and the offline `mutineer-qwkgen` repeats the same `/tmp/qwk_<pid>` plus `zip` pattern. Handles, configured output paths, and archive filenames need strict sanitization and non-shell archive handling. | `src/qwk.c`, `src/tools/mutineer-qwkgen.c`, temp-file policy, archive library |
| P1 | 45% | Incomplete | Message attachment download trusts `msg.file_attached` as a path component under `data/attachments/` without basename validation or canonical root checks. A crafted or corrupted attachment name can traverse outside the attachment directory and be handed to a download protocol. | `src/msg_cmds.c`, message attachment storage, filename sanitizer |
| P1 | 50% | Incomplete | Several database delete helpers pass placeholders to `db_exec()` without binding values. Vote choice cleanup has a corrected second pass, but Fido echolink and QWK hub deletes leave queue/link rows behind because `DELETE ... WHERE ... = ?1` is executed as raw SQL. | `src/db.c`, Fido echolink delete tests, QWK hub delete tests, orphan cleanup |
| P1 | 55% | Incomplete | Archive operations are shell-command wrappers. View/test/extract use `popen()` with command strings and temp directories under `/tmp`; archive conversion and virus-scan hooks are still listed as missing in parity docs. | `src/file_cmds.c`, `docs/PARITY.md`, external archive/AV tools |
| P1 | 60% | Incomplete | Split-screen chat has two competing implementations. `src/chat.c` contains a proper scroll-region split-chat path, but the menu action `splitchat` in `src/session.c` still uses the rapid-refresh buffer path called out as partial in `docs/PARITY.md`. | `src/chat.c`, `src/session.c`, `menus/chat.mnu`, `docs/PARITY.md` |
| P1 | 20% | Incomplete | COVE is largely a placeholder despite being documented as a hub daemon. `coved` main loop calls functions that return zero after comments like "would need proper query implementation"; subscription, area authority, moderation fanout, upstream queueing, health checks, and cleanup are stubs. | `src/tools/coved.c`, `docs/networking-plank.md`, `docs/PLANK_ADMIN.md`, COVE tables |
| P1 | 55% | Incomplete | Online-session notifications can race with disconnect cleanup. Short-message notification stores a `Session*` after releasing `g_online_mu`, then writes to `target->fd`; a concurrent logout can remove, close, and free that session before the write. Broadcast keeps the lock while writing, so the codebase has inconsistent lifetime rules. | `src/session.c`, online user registry, notification locking model |
| P1 | 60% | Incomplete | Accepted telnet sessions do not handle per-session database-open failure. `net_run_listener()` allocates a `Session`, calls `db_open(cfg->db_path)`, and starts `session_thread_main()` without checking `s->db`; cleanup and most session paths assume a valid DB pointer. | `src/net_listener.c`, `src/session.c`, DB error handling |
| P1 | 55% | Incomplete | Password recovery is weaker than normal password-change flows. The reset path offers a security question after one bad password, has no separate throttle for recovery answers, does not confirm the new password, does not enforce the registration minimum length, uses `db_user_set_pw()` instead of timestamped password update, and leaves the caller at the login prompt without forcing a fresh authenticated session. | `src/session.c`, `src/db.c`, password recovery tests |
| P2 | 70% | Incomplete | Menu/UI depth and ANSI art pack remain incomplete. Parity docs list deeper per-area menu trees pending and file/message/door/voting screens still using inline text rather than the completed green ANSI art approach. | `menus/*.mnu`, `menus/*.ans`, `art/`, `docs/PARITY.md` |
| P2 | 75% | Incomplete | Configuration is file-based only. Parity docs mark no interactive config editor, so sysops cannot manage all runtime settings from inside the BBS despite other editors existing for protocols/events/votes/conferences. | `src/config.c`, `src/session.c`, sysop menus |
| P2 | 45% | Incomplete | Menu command flags are parsed, saved, and documented but not enforced when a user selects an item. `CMD_FLAG_PASSWORD` and `CMD_FLAG_SYSOP_LOG` are preserved by `menu_load()`/`menu_save()`, but the session loop checks only ACS before calling `handle_action()`. Menu passwords and per-command sysop logging are inert. | `src/menu.c`, `src/session.c`, `include/bbs_menu.h`, `docs/menus-and-ui.md` |
| P2 | 70% | Incomplete | Logging/audit coverage is partial. Core logs, trap/audit tables, and chat logs exist, but parity docs still call for more detail. Security-sensitive actions such as external process launch and protocol transfers need richer structured audit entries. | `src/log.c`, `src/db.c`, `src/doors.c`, `src/plugin_loader.c`, `docs/PARITY.md` |
| P2 | 70% | Incomplete | PLANK core has useful unit coverage, but `coved` contains unused handler skeletons and PLANK integration with normal BBS message posting is documented more strongly than the visible source proves. End-to-end node-to-node daemon tests are still missing. | `src/tools/coved.c`, `src/plank/*`, `src/msg_cmds.c`, PLANK daemon integration tests |
| P2 | 75% | Incomplete | Build warnings are not treated as release gates. Fresh build emits unused-code warnings in chat/COVE and deprecated OpenSSL SHA256 warnings in PLANK crypto. These are not fatal today, so drift can accumulate. | `CMakeLists.txt`, `src/chat.c`, `src/tools/coved.c`, `src/plank/plank_crypto.c` |
| P2 | 80% | Incomplete | Docker/quick-start path exists but was not validated by the CTest suite. README advertises `docker compose up -d` and default sysop credentials, while expect tests failed against the running daemon credentials. This needs a repeatable E2E smoke path matching user-facing startup. | `Dockerfile`, `docker-compose.yml`, `docker/`, README, expect test seed data |
| P2 | 90% | Incomplete | `who` action sends the rendered online-user list twice, creating duplicate output for callers and brittle transcript tests. | `src/session.c`, who/online user tests |
| P2 | 65% | Incomplete | In-BBS maintenance purge commands are coarse and weakly validated. Message purge accepts any numeric value including zero/negative-like input through `atoi()`, runs destructive deletes outside an explicit transaction/preview, and file purge deletes records without validating corresponding disk cleanup or area scope. | `src/session.c`, `src/maint.c`, sysop maintenance workflow |
| P2 | 60% | Incomplete | Guest logins bypass normal login throttling and user accounting. The guest branch returns before `login_throttled()`, creates a temporary user with `id=0`, and then participates in node, call-log, stats, and logon-event paths that expect persistent users. | `src/session.c`, auth throttling, guest policy, call logs |
| P2 | 55% | Incomplete | Scheduler and WFC shutdown are detached-thread best-effort flows. The scheduler receives the main DB pointer and is never joined or explicitly stopped before `db_close(db)`; WFC is detached and `wfc_stop()` only sleeps for 200 ms. Shutdown can race DB use and terminal cleanup. | `src/main.c`, `src/scheduler.c`, `src/wfc.c`, thread lifecycle |
| P2 | 60% | Incomplete | WFC provides an unaudited privileged shell escape through `D`, hardcoded to `/bin/sh`. There is no config gate, audit record, restricted shell choice, or lifecycle coordination with active BBS shutdown. | `src/wfc.c`, sysop console policy, audit logging |
| P2 | 70% | Incomplete | `mutineer-maint backup` falls back from SQLite `VACUUM INTO` to shelling out `cp '<db>' '<backup>'`. Paths are interpolated into a shell string, so backup output and DB paths need escaping or a non-shell copy implementation. | `src/tools/mutineer-maint.c`, maintenance tools, backup tests |
| P2 | 65% | Incomplete | File validation is mostly cosmetic. Uploads set `FILE_FLAG_NOTVAL`, but normal list/search/new-scan/download paths still show and transfer unvalidated files unless sysops manually remove them. Validation should hide or block unapproved files from non-sysops and should be covered by E2E tests. | `src/file_cmds.c`, `src/session.c`, `src/db.c`, file validation workflow |
| P2 | 65% | Incomplete | User-specific start menus and validation-level menu changes are stored but not honored by login. `users.user_start_menu` and `validation_levels.new_menu` are read/written in DB helpers, but the session always loads `cfg.menu_main` after authentication. Validation workflows cannot actually route users to alternate menus. | `src/session.c`, `src/db.c`, validation levels, menu loading |
| P3 | 60% | Planned | Load, soak, and sanitizer test coverage are roadmap-level only. The codebase supports many concurrent nodes and external child processes, but there is no visible load test, ASAN CI lane, or disconnect/child-process stress suite in CTest. | `CMakeLists.txt`, CI scripts, `tests/`, runtime process supervision |
| P3 | 70% | Incomplete | PLANK memory import/export helpers write temporary bundles to hardcoded `/tmp/plank_bundle_XXXXXX` instead of honoring `TMPDIR`, configured data paths, or a test/runtime-specific temp root. This is less severe than command injection, but it complicates chroot/container deployments and test isolation. | `src/plank/plank_bundle.c`, deployment/runtime temp policy |
| P3 | 75% | Incomplete | Documentation status is inconsistent. `README.md` markets full message/file/door/PLANK/Buccaneer capabilities, `TODO.md` marks nearly everything complete, while `BUCC_TODO.md` and `docs/PARITY.md` identify major unresolved areas. Some docs also call Buccaneer a VM even though the product framing should be an interpreted language for Mutineer addons/games/extensions. Docs need a single source of truth. | `README.md`, `TODO.md`, `BUCC_TODO.md`, `docs/PARITY.md`, Buccaneer docs, generated website docs |
