# Mutineer TODO

Current TODOs should be represented as source-linked issues, tests, or rows in
the feature matrix. This file remains a lightweight index for review follow-up
items that do not yet need a dedicated issue.

- Keep status artifacts under `docs/status/` instead of the repository root.
- Keep generated website output reproducible from Markdown documentation.
